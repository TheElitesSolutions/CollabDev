# =================================
# CollabDev+ Production Environment
# =================================
# SECURITY WARNING: Use strong, unique passwords in production!
# Generate secrets with: openssl rand -base64 32

# =================================
# General Settings
# =================================
NODE_ENV=production
TZ=UTC

# =================================
# PostgreSQL Database
# =================================
# Use a managed database service in production (e.g., AWS RDS, Azure Database)
POSTGRES_USER=collabdev_prod
POSTGRES_PASSWORD=GENERATE_STRONG_PASSWORD_HERE
POSTGRES_DB=collabdev_prod
POSTGRES_PORT=5432
# For managed databases:
# DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# =================================
# Redis Cache
# =================================
# Use a managed Redis service in production (e.g., AWS ElastiCache, Redis Cloud)
REDIS_PORT=6379
REDIS_PASSWORD=GENERATE_STRONG_PASSWORD_HERE
# For managed Redis:
# REDIS_URL=redis://:password@host:6379

# =================================
# Neo4j Graph Database (Optional)
# =================================
NEO4J_USER=neo4j
NEO4J_PASSWORD=GENERATE_STRONG_PASSWORD_HERE
NEO4J_HTTP_PORT=7474
NEO4J_BOLT_PORT=7687

# =================================
# API Service
# =================================
API_PORT=3001
CORS_ORIGIN=https://your-domain.com

# Better Auth Configuration
# IMPORTANT: Generate a strong secret for production!
BETTER_AUTH_SECRET=GENERATE_32_CHAR_SECRET_HERE
BETTER_AUTH_URL=https://api.your-domain.com

# =================================
# Web Service
# =================================
WEB_PORT=3000
NEXT_PUBLIC_API_URL=https://api.your-domain.com

# =================================
# SSL/TLS Configuration
# =================================
# For reverse proxy (nginx, traefik, etc.)
SSL_CERT_PATH=/etc/ssl/certs/your-domain.crt
SSL_KEY_PATH=/etc/ssl/private/your-domain.key

# =================================
# Monitoring & Observability
# =================================
SENTRY_DSN=https://your-sentry-dsn
SENTRY_ENVIRONMENT=production

# =================================
# Rate Limiting
# =================================
THROTTLER_ENABLED=true
THROTTLER_LIMIT=100
THROTTLER_TTL=60

# =================================
# Docker Registry
# =================================
REGISTRY=ghcr.io
IMAGE_NAME=your-org/collabdev
IMAGE_TAG=latest

# =================================
# Scaling Configuration
# =================================
# Number of API replicas (for docker swarm/k8s)
API_REPLICAS=2
WEB_REPLICAS=2
